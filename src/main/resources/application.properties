#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.jpa.hibernate.ddl-auto=create
#spring.datasource.url=jdbc:postgresql://${POSTGRES_SERVICE}:5432/${POSTGRES_DB_NAME}
#spring.datasource.username=${POSTGRES_DB_USER}
#spring.datasource.password=${POSTGRES_DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.datasource.url = jdbc:postgresql://${POSTGRES_DB_HOST:192.168.99.100}:\
  ${EXPOSED_POSTGRES_PORT:5432}/employees
spring.datasource.username = ${POSTGRES_USER:postgres}
spring.datasource.password = ${POSTGRES_PASSWORD:abc123}
server.port=8051